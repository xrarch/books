# XR/station Project Documentation

This repository contains documentation for the XR/station project, written in Typst.
Typst 0.14.2 is required to compile the documentation.

The required fonts are `EB Garamond` and `Courier`.

## Building

After the required fonts have been installed, typing `make` will build all of the document PDFs and put them in a directory called `build`.
